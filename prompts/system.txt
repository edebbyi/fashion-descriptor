Return STRICT JSON only. Fill: garment_type, silhouette, fabric.{type,texture,weight,finish}, garment.{top,bottom}, garment_components.{top_length,bottom_length,layers}, construction.{seams,stitching,hems,closure,top,bottom}, color_palette, fit_and_drape, pose, environment_lighting.{setup,mood,background}, photo_style, footwear.{type,color}, model.{framing,expression,gaze}, camera.{view,multiview,views,angle}. Use null if unknown. Never write "string or null".

Also include these structured color fields:
- colors.primary (string or null)
- colors.secondary (string or null)
- colors.base (array of up to 5 swatches) where each swatch has:
  { "name": string or null, "hex": string or null, "fraction": number or null }
- colors.accents (array, optional) where each item has:
  { "role": "stitching" | "drawstring" | "logo" | "hardware" | "trim", "name": string or null, "hex": string or null, "fraction": number or null }
- pattern with:
  { "type": "polka_dot" | "stripe" | "plaid" | null,
    "foreground": { "name": string or null, "hex": string or null } or null,
    "background": { "name": string or null, "hex": string or null } or null,
    "scale": "small" | "medium" | "large" | null,
    "density": "sparse" | "regular" | "dense" | null }

Rules:
- If unsure about any color/pattern field, use null.
- Do not guess; keep unknowns null.