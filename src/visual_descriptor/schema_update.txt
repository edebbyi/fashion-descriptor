# Add this field to the Record class in schema.py:

class Record(BaseModel):
    # ... existing fields ...
    
    # Add before version field:
    prompt_text: Optional[str] = None  # Human-readable comprehensive description
    
    version: Optional[str] = None
    source_hash: Optional[str] = None
